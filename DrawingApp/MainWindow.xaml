<Window x:Class="DrawingApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:DrawingApp"
        mc:Ignorable="d"
        Title="DrawItYourself" Height="500" Width="800" Background="#FFF5ECEA">
    <Window.Resources>

    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="MediaCommands.Play" CanExecute="PlayMusic_CanExecute" Executed="btnPlayMusic_Click" />
        <CommandBinding Command="MediaCommands.Pause" CanExecute="PauseMusic_CanExecute" Executed="btnPauseMusic_Click" />
        <CommandBinding Command="MediaCommands.Stop" CanExecute="StopMusic_CanExecute" Executed="btnStopMusic_Click" />
    </Window.CommandBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="6*"/>
        </Grid.ColumnDefinitions>
        <StackPanel  Grid.Column="0" Orientation="Vertical" Background="blue" >
            <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center" Margin="0,10,0,10">
                <Label Content="Size" Foreground="White" FontWeight="bold" Margin="10,0" />
                <ComboBox  x:Name="thicknessComboBox" 
                           SelectionChanged="ThicknessComboBox_SelectionChanged"
                           Width="100">
                    <ComboBoxItem Content="Small" Tag="1" />
                    <ComboBoxItem Content="Medium" Tag="5" />
                    <ComboBoxItem Content="Large" Tag="10" />
                </ComboBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center" Margin="0,10,0,10">
                <Label Content="Color" Foreground="White" FontWeight="bold" Margin="10,0" />
                <xctk:ColorPicker x:Name="colorPicker"  Width="100" SelectedColorChanged="ColorPicker_SelectedColorChanged"/>
            </StackPanel>
            <StackPanel Orientation="Vertical"  HorizontalAlignment="Center" Margin="0,30,0,0" >
                <Button Content="Clear"  Click="ClearButton_Click" />
                <Button Content="Line Mode" Click="LineModeButton_Click" />
                <Button Content="Ellipse" Click="EllipseButton_Click"/>
                <Button x:Name="RectangleButton" Content="Rectangle" Click="RectangleButton_Click"/>
                <Button x:Name="PencilButton" Content="Pencil"  Click="PencilButton_Click"/>
                <Button Content="Save" Click="SaveButton_Click"  />
            </StackPanel>
            <StackPanel Orientation="Vertical"  HorizontalAlignment="Center" Margin="0,30,0,0" >
                <Label Name="lblStatus" Content="Not playing..." HorizontalContentAlignment="Center" Margin="5" />
                <ToolBar Height="40">
                    <Button Width="40" Name="btnPlayMusic" Command="MediaCommands.Play">
                        <Image Source="./assets/play.png" />
                    </Button>
                    <Separator />
                    <Button Width="40" Name="btnPauseMusic" Command="MediaCommands.Pause">
                        <Image Source="./assets/pause.png" />
                    </Button>
                    <Button Width="40" Name="btnStopMusic" Command="MediaCommands.Stop">
                        <Image Source="./assets/stop.png" />
                    </Button>
                </ToolBar>
                <Slider x:Name="sliderVolume"  Height="24" Width="164" Maximum="100"  Value="{Binding ElementName=mediaPlayer, Path=Volume}" ValueChanged="sliderVolume_ValueChanged" SmallChange="1" />
            </StackPanel>
        </StackPanel>
        <GridSplitter Background="Silver" Grid.Column="1" HorizontalAlignment="Stretch"/>

        <StackPanel Background="Silver" Grid.Column="2">
            <StackPanel x:Name="drawingPageContainer" Width="300">
                <Canvas x:Name="drawingPage" Background="White" MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
MouseMove="Canvas_MouseMove" MouseLeftButtonUp="Canvas_MouseLeftButtonUp" Width="300" Height="300" />
                <Thumb HorizontalAlignment="Right" Name="myThumb" Canvas.Left="80" Canvas.Top="80" Background="Blue" 
      Width="20" Height="20" DragDelta="onDragDelta" 
      DragStarted="onDragStarted" DragCompleted="onDragCompleted"
      />
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>
